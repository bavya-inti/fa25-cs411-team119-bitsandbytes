# Google Cloud SQL Configuration
DB_HOST=your-cloud-sql-instance-ip
DB_PORT=5432
DB_NAME=cs_degree_planner
DB_USER=your-db-user
DB_PASSWORD=your-db-password

# SSL Configuration (set to 'false' to disable SSL)
DB_SSL=true

# For Cloud SQL with private IP, use the instance connection name
INSTANCE_CONNECTION_NAME=project:region:instance-name

# Server Configuration
PORT=3001
NODE_ENV=development

# Require database connection to start server (set to 'true' to exit if DB fails)
REQUIRE_DB=false

# CORS Configuration
FRONTEND_URL=http://localhost:3000

